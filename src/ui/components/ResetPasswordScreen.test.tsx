import { describe, it } from 'vitest'

// ResetPasswordScreen was removed along with password sign-in — see
// ResetPasswordScreen.tsx. This file is kept as an empty stub (rather than
// deleted) because the sandbox this repo is edited in does not allow
// deleting files.
describe.skip('ResetPasswordScreen (removed)', () => {
  it('no longer applies — password sign-in was removed', () => {})
})
